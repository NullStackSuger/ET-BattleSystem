using NPBehave;

namespace ET.Server
{

    public static class TreeFactory
    {
        private const string FilePath = "";
        
        /// <summary>
        /// 构建运行时行为树
        /// </summary>
        /// <param name="treeName">之后用enum表示, 编辑器构建时加一个enum元素</param>
        /// <returns></returns>
        public static Node BuildTree(string treeName)
        {
            return null;
        }
    }
}